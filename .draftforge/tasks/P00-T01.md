# P00-T01 — Create and verify the foundation scaffold

Status: done

## Objective

Create the project structure, decisions, cross-harness state contract, and runnable TypeScript skeleton.

## Owned paths

- Repository-wide foundation files.

## Acceptance criteria

- `npm run typecheck` passes.
- `npm test` passes.
- `npm run session:check` passes.
- `npm run build` produces `dist/cli.js`.
- Codex and Claude Code startup files point to the same canonical state.

## Verification

Run `npm run check`.
