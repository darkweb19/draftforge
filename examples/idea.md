# Example idea

> Placeholder example only. Replace all content before using it for a real project.

Build a command-line tool that reads notes and creates a searchable local archive.

It should work offline, avoid a database server, and produce files that another coding agent can understand.
